# Ghostinject
Ghostinject – Compact, persistent RFI payloads for Linux (PHP), Windows (PHP), and Windows IIS (ASPX). Provides target recon, command execution, and auto‑reconnecting reverse shells. Small size, multi‑method fallbacks, and stealthy operation. For authorized security testing only.
